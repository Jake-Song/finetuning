[project]
name = "finetuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch==2.9.1",
    "trl[vllm]",
    "aiohttp",
    "omegaconf",
    "pyyaml",
    "datasets",
    "accelerate",
    "huggingface-hub",
    "wandb",
]
