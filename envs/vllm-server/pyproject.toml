[project]
name = "finetuning-vllm-server"
version = "0.1.0"
description = "Dedicated vLLM server environment for RL rollout generation"
readme = "../../README.md"
requires-python = ">=3.10"
dependencies = [
    "vllm==0.19.0; sys_platform != 'win32'",
    "transformers<=4.57.5",
]
